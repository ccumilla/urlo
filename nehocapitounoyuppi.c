#include <stdio.h>
int main()
{
char stringa[100];


while(stringa[0]!= 'S' || stringa[1]!='T' || stringa[2]!='O' || stringa[3]!='P')
    {
    printf("inserire una stringa: ");
scanf(" %s", stringa);
printf(" %s\n", stringa);

}
}
