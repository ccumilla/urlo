#include <stdio.h>

int main()
{ 
    int x;
    char genere;
printf("quanti anni hai?" "in che genere ti identifichi?");
scanf ("%d %c", &x, &genere);
printf("ho %d anni, " "sono %c \n", x, genere);
}